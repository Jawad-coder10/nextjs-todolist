const environment = {
    api: {
        rest: {
            baseUrl: 'http://localhost:8081/api',
            endpoints: {
                todolists: 'todolists',
            },
        },
    },
};

export default environment;
